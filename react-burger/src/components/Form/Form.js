import Styles from "./Form.module.css";
import { useRouteMatch } from "react-router-dom";

const From = () => {
    // const { params, isExact, url } = useRouteMatch();
    // console.log(params, isExact, url)
    return (
        <div className={Styles.form}>

        </div>
    )
}

export default From;